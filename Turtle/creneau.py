
from turtle import *
size = 10
for i in range(4):
    right(90)
    forward(size)
    for j in range(6):
        left(90)
        forward(size)
        right(90)
        forward(size)
        right(90)
        forward(size)
        left(90)
        forward(size)
done()