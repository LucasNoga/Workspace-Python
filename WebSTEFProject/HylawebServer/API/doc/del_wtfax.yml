Suppression du fax 
---
tags:
  - fax
description: Supprime les fichiers FAX {filename}.[ctl, ps, pdf] en attente dans le dossier WT
parameters:
  - name: filename
    in: path
    type: string
    required: true
    description: Le nom du fichier fax (sans extension)
responses:
  '200':
    description: "l'operation s'est correctement effectuée"
    schema:
      $ref: '#/definitions/json'
    examples:
      title: "Send Fax (delete)"
      description: ""
      status: "OK"
      items: ""