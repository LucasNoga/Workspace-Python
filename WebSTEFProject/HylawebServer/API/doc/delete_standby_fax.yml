Suppression d'un fax en attente
---
tags:
  - standby
description: Supprime le fax du dossier STANDBY
parameters:
  - name: filename
    in: query
    type: string
    required: true
    description: nom du fichier à mettre en attente

responses:
  '200':
    description: Suppression d'un fax en attente
    schema:
      $ref: '#/definitions/json'
    examples:
      title: "Fax mis en attente"
      description: "Suppression du fichier en attente XXXX.pdf"
      status: "OK"