Affiche le fax au format PDF 
---
tags:
  - fax
description: Affiche le fax en attente dans le dossier WT
parameters:
  - name: filename
    in: path
    type: string
    required: true
    description: Le nom du fichier fax (sans extension)
produces:
  - application/pdf
responses:
  '200':
    description: A PDF file
    schema:
      type: file